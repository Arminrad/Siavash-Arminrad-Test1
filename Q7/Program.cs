using System;

namespace Q7
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("ref & out both are used for reference type variables, but if we use \"ref\", the variable must be defined first, before " +
                "assigning to the arguments of a function or method, although we can assign an \"out\" variable without defining it beforehand!");
        }
    }
}
